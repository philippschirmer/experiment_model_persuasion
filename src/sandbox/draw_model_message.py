# TODO: Function that "draws" a specified model message into data


def draw_model_message(data, model, hyperparams):
    ''' TODO: function takes as input prespecified data and model to be drawn into it.
        As an output, generates a picture file and saves it to prespecified location.

        Inputs:
        data:  2xT (2D) np.array, containing dependent and independent variable.
        model: integer  - model message to be sent.
        hyperparams (optional?) - hyperparameters to be used in generating the picture.

        Outputs:

        message_`data'_`model'.png - model message as picture file.

        TODO: Save directly as part of the function? Then no real output
    
    
    '''
    pass